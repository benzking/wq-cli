# wq-cli CLI 标准化 + Agent 集成 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在 wq-cli 项目内创建标准化 `wq` CLI（stdlib core + 可选 deps），暴露 `pyproject.toml` 入口点；创建 OpenClaw + Hermes 两个 Agent Skill；支持 `--format=md|mhtml|table|json` 多格式输出。

**架构：** `cli/` 包（8 模块单向依赖树）→ `pyproject.toml` 入口点 → Agent 通过 `wq <cmd>` 调用 HTTP API。`core.py` 是单向依赖根节点，`__init__.py` 是唯一组装点。两个 Agent Skill 通过 `writing-skills` 创建，渐进式披露。

**技术栈：** Python 3.10+ (stdlib: urllib, sqlite3, argparse, json, concurrent.futures, tempfile, hashlib, base64)
可选依赖: tabulate, html2text。测试: pytest。打包: setuptools + pyproject.toml。

---

## 文件清单

### 创建

| # | 文件 | 职责 |
|---|------|------|
| 1 | `cli/__init__.py` | main() + argparse 子命令路由 + `--format` + parse_known_args + cron-setup + version |
| 2 | `cli/core.py` | _req, _db_conn (四级回落), _ok, _fail, _ensure_service, _download_images |
| 3 | `cli/health.py` | cmd_check (含 --auto-recover), cmd_status |
| 4 | `cli/search.py` | cmd_search, cmd_info |
| 5 | `cli/subscribe.py` | cmd_subscribe, cmd_unsubscribe, cmd_subscriptions, cmd_poll |
| 6 | `cli/articles.py` | cmd_articles (SQLite + API 兜底) |
| 7 | `cli/fetch.py` | cmd_fetch (json/md/mhtml 渲染, 图片本地化, _WECHAT_BASE_CSS) |
| 8 | `cli/push.py` | cmd_push_report, cmd_md_push (均含 --hours) |
| 9 | `cli/login.py` | cmd_login (引导浏览器) |
| 10 | `pyproject.toml` | 打包配置 + [project.scripts] wq + optional-dependencies |
| 11 | `tests/test_core.py` | DB 回落 + HTTP mock + 图片下载白名单 |
| 12 | `tests/test_health.py` | check + status 集成测试 |
| 13 | `tests/test_search.py` | search + info 集成测试 |
| 14 | `tests/test_subscribe.py` | 订阅4命令集成测试 |
| 15 | `tests/test_articles.py` | articles 查询 + API 兜底 |
| 16 | `tests/test_fetch.py` | fetch json/md/mhtml |
| 17 | `tests/test_push.py` | push-report + md-push |
| 18 | `tests/test_login.py` | login 单元测试 |
| 19 | `skills/openclaw/SKILL.md` | OpenClaw L1+L2 渐进式披露 |
| 20 | `skills/openclaw/opencli.json` | OpenCLI manifest |
| 21 | `skills/openclaw/references/troubleshooting.md` | 错误码速查 |
| 22 | `skills/hermes/SKILL.md` | Hermes L1+L2 渐进式披露 |
| 23 | `skills/hermes/references/cron-setup.md` | cron 详细指引 |
| 24 | `skills/shared/commands-schema.json` | canonical 入参/出参定义 |
| 25 | `skills/shared/article-formats.md` | md/mhtml 格式说明 |

### 修改

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `~/.hermes/skills/wechat-query/SKILL.md` | `python3 $WQ` → `wq`, 标注 deprecated |
| 2 | `~/.hermes/scripts/wechat-inspection.sh` | 改为 `/home/gly/wq-cli/venv/bin/wq check` |
| 3 | `~/.hermes/scripts/wechat-daily-push.sh` | 改为 `/home/gly/wq-cli/venv/bin/wq md-push --hours=24` |

### 不变

wq-cli 所有后端代码（`app.py`, `routes/*.py`, `utils/*.py`）完全不变。

---

### 任务 1：pyproject.toml 打包配置

**文件：**
- 创建：`/home/gly/wq-cli/pyproject.toml`

- [ ] **步骤 1：编写 pyproject.toml**

```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "wq-cli"
version = "0.1.0"
description = "WeChat Article Query CLI — Agent interface for wq-cli service"
requires-python = ">=3.10"

[project.optional-dependencies]
table = ["tabulate"]
md = ["html2text"]
all = ["tabulate", "html2text"]

[project.scripts]
wq = "cli:main"
```

- [ ] **步骤 2：验证 pyproject.toml 语法**

运行：`/home/gly/wq-cli/venv/bin/pip install -e /home/gly/wq-cli 2>&1 | head -20`
预期：因为 `cli/` 包还不存在会报错，但 toml 语法本身不应报错

- [ ] **步骤 3：Commit**

```bash
cd /home/gly/wq-cli
git add pyproject.toml
git commit -m "feat: add pyproject.toml with wq entry point"
```

---

### 任务 2：cli/core.py 基础工具层

**文件：**
- 创建：`/home/gly/wq-cli/cli/__init__.py`（最小 stubs）
- 创建：`/home/gly/wq-cli/cli/core.py`
- 创建：`/home/gly/wq-cli/tests/test_core.py`

- [ ] **步骤 1：编写最小 `cli/__init__.py` stub 使 pip install 成功**

```python
"""wq-cli CLI — WeChat Article Query command line interface."""

def main():
    print("wq-cli v0.1.0")
```

- [ ] **步骤 2：验证 pip install 成功**

运行：
```bash
cd /home/gly/wq-cli && /home/gly/wq-cli/venv/bin/pip install -e . 2>&1
/home/gly/wq-cli/venv/bin/wq 2>&1
```
预期：输出 `wq-cli v0.1.0`

- [ ] **步骤 3：编写 `tests/test_core.py` — HTTP mock 测试**

```python
"""Tests for cli.core — HTTP request, DB fallback, image download."""
import io
import json
import os
import sqlite3
import tempfile
import unittest
from unittest.mock import patch, MagicMock

# Force no tabulate to test fallback path
import sys
sys.modules['tabulate'] = None

from cli.core import _ok, _fail, _ensure_service, _download_images


class TestOkFail(unittest.TestCase):
    def test_ok_outputs_json_and_exits_0(self):
        with self.assertRaises(SystemExit) as cm:
            _ok({"msg": "hello"})
        self.assertEqual(cm.exception.code, 0)

    def test_fail_outputs_json_and_exits_1(self):
        with self.assertRaises(SystemExit) as cm:
            _fail("something broke")
        self.assertEqual(cm.exception.code, 1)

    def test_fail_custom_exit_code(self):
        with self.assertRaises(SystemExit) as cm:
            _fail("auth expired", exit_code=2)
        self.assertEqual(cm.exception.code, 2)


class TestDBConn(unittest.TestCase):
    def setUp(self):
        self.tmpdir = tempfile.mkdtemp()
        self.db_path = os.path.join(self.tmpdir, "test.db")
        conn = sqlite3.connect(self.db_path)
        conn.execute("CREATE TABLE IF NOT EXISTS config (key TEXT PRIMARY KEY, value TEXT)")
        conn.execute("INSERT OR REPLACE INTO config VALUES ('test', 'hello')")
        conn.commit()
        conn.close()
        # Simulate read-only by removing write permission
        os.chmod(self.db_path, 0o444)

    def tearDown(self):
        os.chmod(self.db_path, 0o644)
        os.unlink(self.db_path)
        os.rmdir(self.tmpdir)

    @patch('cli.core.DB_PATH', new_callable=lambda: None)
    def test_level2_readonly_fallback(self, mock_path):
        """When dir is not writable, falls back to query_only=ON."""
        from cli.core import _db_conn, DB_PATH
        # Override DB_PATH for test
        import cli.core as core_mod
        old_path = core_mod.DB_PATH
        core_mod.DB_PATH = self.db_path
        try:
            conn = _db_conn()
            self.assertIsNotNone(conn)
            row = conn.execute("SELECT value FROM config WHERE key='test'").fetchone()
            self.assertEqual(row[0], 'hello')
            conn.close()
        finally:
            core_mod.DB_PATH = old_path


class TestDownloadImages(unittest.TestCase):
    def setUp(self):
        self.outdir = tempfile.mkdtemp()

    def tearDown(self):
        import shutil
        shutil.rmtree(self.outdir)

    def test_allowed_domain_passes(self):
        urls = ["https://mmbiz.qpic.cn/mmbiz_jpg/abc123/0"]
        with patch('cli.core._download_one_image') as mock_dl:
            mock_dl.return_value = ("mmbiz.qpic.cn", "/tmp/img/abc123.jpg")
            result = _download_images(urls, self.outdir)
        self.assertIn("https://mmbiz.qpic.cn/mmbiz_jpg/abc123/0", result)

    def test_blocked_domain_rejected(self):
        urls = ["https://evil.com/malware.exe"]
        with self.assertRaises(ValueError) as cm:
            _download_images(urls, self.outdir)
        self.assertIn("evil.com", str(cm.exception))

    def test_non_image_magic_bytes_rejected(self):
        """Non-image file should be rejected by magic byte check."""
        # Tested via _validate_image_bytes directly
        from cli.core import _validate_image_bytes
        self.assertFalse(_validate_image_bytes(b"#!/bin/bash\necho pwned"))
        self.assertTrue(_validate_image_bytes(b"\xff\xd8\xff\xe0\x00\x10JFIF"))
```

- [ ] **步骤 4：运行测试确认失败**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_core.py -v 2>&1 | tail -20`
预期：部分失败（core.py 尚未实现完整功能）

- [ ] **步骤 5：编写 `cli/core.py` 完整实现**

```python
"""wq-cli core — HTTP, DB fallback, output, image download. stdlib only."""
import concurrent.futures
import hashlib
import json
import os
import shutil
import sqlite3
import sys
import tempfile
import urllib.error
import urllib.request

API = os.environ.get("WECHAT_API", "http://localhost:5000/api")
SERVICE_DIR = os.environ.get("WECHAT_SERVICE_DIR", "/home/gly/wq-cli")
DB_PATH = os.path.join(SERVICE_DIR, "data", "rss.db")

IMAGE_CDN_DOMAINS = frozenset({
    "mmbiz.qpic.cn",
    "mmbiz.qlogo.cn",
    "mmecoa.qpic.cn",
    "mp.weixin.qq.com",
})

IMAGE_MAGIC = {
    b'\xff\xd8\xff': 'jpg',
    b'\x89PNG':    'png',
    b'GIF8':       'gif',
    b'RIFF':       'webp',
    b'<?xml':      'svg',
    b'<svg':       'svg',
}

VERSION = "0.1.0"


def _req(method, path, body=None):
    """HTTP request via urllib (stdlib). Returns (status_code, dict)."""
    url = f"{API}{path}"
    data = None
    headers = {"Accept": "application/json", "User-Agent": f"wq-cli/{VERSION} (Agent)"}
    if method == "POST" and body is not None:
        data = json.dumps(body).encode()
        headers["Content-Type"] = "application/json"
    req = urllib.request.Request(url, data=data, headers=headers, method=method)
    try:
        with urllib.request.urlopen(req, timeout=15) as resp:
            raw = resp.read().decode()
            return resp.status, json.loads(raw) if raw else {}
    except urllib.error.HTTPError as e:
        raw = e.read().decode() if e.fp else "{}"
        try:
            return e.code, json.loads(raw)
        except json.JSONDecodeError:
            return e.code, {"error": raw[:200]}
    except Exception as e:
        return 0, {"error": str(e)}


def _db_conn():
    """Four-level DB access fallback. Returns sqlite3.Connection or None."""
    if not os.path.isfile(DB_PATH):
        return None
    db_dir = os.path.dirname(DB_PATH)

    # Level 1: direct read/write
    if os.access(db_dir, os.W_OK) and os.access(DB_PATH, os.R_OK):
        conn = sqlite3.connect(DB_PATH)
        conn.row_factory = sqlite3.Row
        return conn

    # Level 2: read-only + memory temp store
    if os.access(DB_PATH, os.R_OK):
        conn = sqlite3.connect(f"file:{DB_PATH}?mode=ro", uri=True)
        conn.execute("PRAGMA query_only=ON")
        conn.execute("PRAGMA temp_store=MEMORY")
        conn.row_factory = sqlite3.Row
        return conn

    # Level 3: copy to /tmp
    try:
        h = hashlib.sha256(DB_PATH.encode()).hexdigest()[:12]
        tmp = os.path.join(tempfile.gettempdir(), f"wechat_rss_{h}.db")
        shutil.copy2(DB_PATH, tmp)
        os.chmod(tmp, 0o600)
        conn = sqlite3.connect(tmp)
        conn.row_factory = sqlite3.Row
        return conn
    except (PermissionError, OSError):
        return None


def _ok(data=None):
    """Print JSON success payload and exit 0."""
    print(json.dumps({"ok": True, "data": data}, ensure_ascii=False))
    sys.exit(0)


def _fail(msg, exit_code=1):
    """Print JSON error payload and exit with given code."""
    print(json.dumps({"ok": False, "error": str(msg)}, ensure_ascii=False))
    sys.exit(exit_code)


def _ensure_service():
    """Check service health, _fail if unreachable."""
    code, d = _req("GET", "/health")
    if code != 200 or d.get("status") != "healthy":
        _fail(f"Service unreachable ({code}). Start: bash /home/gly/wq-cli/start.sh")


def _format_table(rows, columns, format_spec="simple"):
    """Render rows as table. Uses tabulate if available, else ASCII fallback."""
    try:
        from tabulate import tabulate
        return tabulate(rows, headers=columns, tablefmt=format_spec)
    except ImportError:
        return _ascii_table(rows, columns)


def _ascii_table(rows, columns):
    """Minimal ASCII table when tabulate is not installed."""
    if not rows:
        return "(empty)"
    col_widths = [len(c) for c in columns]
    for row in rows:
        for i, cell in enumerate(row):
            col_widths[i] = max(col_widths[i], len(str(cell)))
    sep = "+" + "+".join("-" * (w + 2) for w in col_widths) + "+"
    header = "|" + "|".join(f" {columns[i]:<{col_widths[i]}} " for i in range(len(columns))) + "|"
    lines = [sep, header, sep]
    for row in rows:
        lines.append("|" + "|".join(f" {str(row[i]):<{col_widths[i]}} " for i in range(len(row))) + "|")
    lines.append(sep)
    return "\n".join(lines)


def _validate_image_bytes(data):
    """Check magic bytes to confirm data is an image."""
    for magic, ext in IMAGE_MAGIC.items():
        if data[:len(magic)] == magic:
            return True
    return False


def _download_images(image_urls, outdir):
    """Download images to outdir. Returns {url: local_path} mapping.

    Raises ValueError for non-whitelisted domains.
    """
    import concurrent.futures
    from urllib.parse import urlparse

    os.makedirs(outdir, exist_ok=True)
    concurrency = int(os.environ.get("WQ_IMAGE_CONCURRENCY", "4"))

    # Validate domains first
    for url in image_urls:
        domain = urlparse(url).netloc.split(":")[0]
        if domain not in IMAGE_CDN_DOMAINS:
            raise ValueError(f"Image domain not allowed: {domain} ({url})")

    def _download_one(url):
        domain = urlparse(url).netloc.split(":")[0]
        ext = ".jpg"
        for suffix in ['.png', '.gif', '.webp', '.svg', '.jpeg', '.jpg']:
            if suffix in url.split('?')[0].lower():
                ext = suffix
                break
        h = hashlib.sha256(url.encode()).hexdigest()[:12]
        fname = f"{h}{ext}"
        fpath = os.path.join(outdir, fname)
        if os.path.exists(fpath):
            return url, fpath
        req = urllib.request.Request(url, headers={"User-Agent": f"wq-cli/{VERSION}"})
        with urllib.request.urlopen(req, timeout=30) as resp:
            data = resp.read()
        if not _validate_image_bytes(data):
            raise ValueError(f"Downloaded file is not a valid image: {url}")
        with open(fpath, 'wb') as f:
            f.write(data)
        os.chmod(fpath, 0o600)
        return url, fpath

    result = {}
    with concurrent.futures.ThreadPoolExecutor(max_workers=concurrency) as executor:
        futures = {executor.submit(_download_one, u): u for u in image_urls}
        for fut in concurrent.futures.as_completed(futures):
            url, local_path = fut.result()
            result[url] = local_path
    return result
```

- [ ] **步骤 6：运行测试确认通过**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_core.py -v 2>&1 | tail -20`
预期：全部 PASS（可能 test_level2_readonly_fallback 需要根据实际权限调整）

- [ ] **步骤 7：Commit**

```bash
cd /home/gly/wq-cli
git add cli/__init__.py cli/core.py tests/test_core.py
git commit -m "feat(cli): add core module with HTTP, DB fallback, image download"
```

---

### 任务 3：cli/__init__.py argparse 主入口

**文件：**
- 修改：`/home/gly/wq-cli/cli/__init__.py`

- [ ] **步骤 1：编写完整 argparse 入口**

```python
"""wq-cli CLI — WeChat Article Query command line interface.

Usage:
  wq check [--auto-recover]
  wq status
  wq search <query> [--format=json|table]
  wq info <fakeid>
  wq subscribe <fakeid> [--nickname=<name>]
  wq unsubscribe <fakeid>
  wq subscriptions [--format=json|table]
  wq poll
  wq articles [--hours=N] [--keyword=K] [--fakeid=F] [--limit=N] [--format=json|table]
  wq fetch <url> [--format=json|md|mhtml] [--outdir=PATH]
  wq push-report [--hours=N]
  wq md-push [--hours=N]
  wq login
  wq cron-setup
  wq version
"""
import argparse
import sys

VERSION = "0.1.0"


def main():
    parser = argparse.ArgumentParser(
        prog="wq",
        description="WeChat Article Query CLI — Agent interface for wq-cli service",
    )
    sub = parser.add_subparsers(dest="command", title="commands")

    # check
    p_check = sub.add_parser("check", help="Health + login status check")
    p_check.add_argument("--auto-recover", action="store_true", help="Try auto-restart on failure")

    # status
    sub.add_parser("status", help="Auth status only (JSON)")

    # search
    p_search = sub.add_parser("search", help="Search WeChat accounts")
    p_search.add_argument("query", nargs="+", help="Search keyword")
    p_search.add_argument("--format", default="json", choices=["json", "table"])

    # info
    p_info = sub.add_parser("info", help="Account detail")
    p_info.add_argument("fakeid", help="Account FakeID")

    # subscribe
    p_sub = sub.add_parser("subscribe", help="Add RSS subscription")
    p_sub.add_argument("fakeid", help="Account FakeID")
    p_sub.add_argument("--nickname", default="", help="Account nickname")

    # unsubscribe
    p_unsub = sub.add_parser("unsubscribe", help="Remove subscription")
    p_unsub.add_argument("fakeid", help="Account FakeID")

    # subscriptions
    p_subs = sub.add_parser("subscriptions", help="List subscriptions")
    p_subs.add_argument("--format", default="json", choices=["json", "table"])

    # poll
    sub.add_parser("poll", help="Trigger RSS poll")

    # articles
    p_arts = sub.add_parser("articles", help="Query cached articles")
    p_arts.add_argument("--hours", type=int, default=None)
    p_arts.add_argument("--keyword", default=None)
    p_arts.add_argument("--fakeid", default=None)
    p_arts.add_argument("--limit", type=int, default=20)
    p_arts.add_argument("--format", default="json", choices=["json", "table"])

    # fetch
    p_fetch = sub.add_parser("fetch", help="Fetch article full content")
    p_fetch.add_argument("url", help="WeChat article URL")
    p_fetch.add_argument("--format", default="json", choices=["json", "md", "mhtml"])
    p_fetch.add_argument("--outdir", default=".", help="Output directory (for md/mhtml)")

    # push-report
    p_pr = sub.add_parser("push-report", help="Push report JSON")
    p_pr.add_argument("--hours", type=int, default=24)

    # md-push
    p_mp = sub.add_parser("md-push", help="Push report Markdown")
    p_mp.add_argument("--hours", type=int, default=24)

    # login
    sub.add_parser("login", help="Show login page URL")

    # cron-setup
    sub.add_parser("cron-setup", help="Print cron registration guide")

    # version
    sub.add_parser("version", help="Show version")

    # Use parse_known_args for OpenCLI compatibility
    args, _ = parser.parse_known_args()

    if args.command is None:
        parser.print_help()
        sys.exit(0)

    if args.command == "cron-setup":
        _cmd_cron_setup()
        return

    if args.command == "version":
        print(f"wq-cli v{VERSION}")
        return

    if args.command == "login":
        _cmd_login()
        return

    # All other commands need service
    from cli.core import _ensure_service
    if args.command != "check":
        _ensure_service()

    _dispatch(args)


def _dispatch(args):
    cmd = args.command
    if cmd == "check":
        from cli.health import cmd_check
        cmd_check(auto_recover=getattr(args, 'auto_recover', False))
    elif cmd == "status":
        from cli.health import cmd_status
        cmd_status()
    elif cmd == "search":
        from cli.search import cmd_search
        cmd_search(" ".join(args.query), fmt=getattr(args, 'format', 'json'))
    elif cmd == "info":
        from cli.search import cmd_info
        cmd_info(args.fakeid)
    elif cmd == "subscribe":
        from cli.subscribe import cmd_subscribe
        cmd_subscribe(args.fakeid, args.nickname)
    elif cmd == "unsubscribe":
        from cli.subscribe import cmd_unsubscribe
        cmd_unsubscribe(args.fakeid)
    elif cmd == "subscriptions":
        from cli.subscribe import cmd_subscriptions
        cmd_subscriptions(fmt=getattr(args, 'format', 'json'))
    elif cmd == "poll":
        from cli.subscribe import cmd_poll
        cmd_poll()
    elif cmd == "articles":
        from cli.articles import cmd_articles
        cmd_articles(
            fakeid=args.fakeid, hours=args.hours,
            keyword=args.keyword, limit=args.limit,
            fmt=getattr(args, 'format', 'json'),
        )
    elif cmd == "fetch":
        from cli.fetch import cmd_fetch
        cmd_fetch(args.url, fmt=args.format, outdir=args.outdir)
    elif cmd == "push-report":
        from cli.push import cmd_push_report
        cmd_push_report(hours=args.hours)
    elif cmd == "md-push":
        from cli.push import cmd_md_push
        cmd_md_push(hours=args.hours)


def _cmd_cron_setup():
    venv_wq = "/home/gly/wq-cli/venv/bin/wq"
    print(f"""\
## Hermes cron 任务注册

### 每日 09:00 服务巡检
cronjob action=create \\
  name="wechat-inspection" \\
  schedule="0 9 * * *" \\
  script={venv_wq} check \\
  no_agent=true \\
  deliver=origin

### 每日 18:00 文章推送
cronjob action=create \\
  name="wechat-daily-push" \\
  schedule="0 18 * * *" \\
  script={venv_wq} md-push --hours=24 \\
  no_agent=true \\
  deliver=origin

### 验证
cronjob action=list

> venv 路径: {venv_wq}
> 如需 OpenCLI cron: opencli cron create ...
""")


def _cmd_login():
    print("""\
请打开管理页面扫码登录:
  http://localhost:5000/login.html

登录后凭证有效期约 4 天。检测到期后用 wq login 重新打开此页面。
""")
```

- [ ] **步骤 2：验证 wq 帮助信息**

运行：
```bash
/home/gly/wq-cli/venv/bin/wq --help 2>&1 | head -10
/home/gly/wq-cli/venv/bin/wq check --help 2>&1
/home/gly/wq-cli/venv/bin/wq version 2>&1
/home/gly/wq-cli/venv/bin/wq cron-setup 2>&1 | head -10
```
预期：帮助信息正确，version 输出 `wq-cli v0.1.0`

- [ ] **步骤 3：Commit**

```bash
cd /home/gly/wq-cli
git add cli/__init__.py
git commit -m "feat(cli): add argparse main entry with all subcommands"
```

---

### 任务 4：cli/health.py 健康检查命令

**文件：**
- 创建：`/home/gly/wq-cli/cli/health.py`
- 创建：`/home/gly/wq-cli/tests/test_health.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.health — check, status."""
from unittest.mock import patch, MagicMock
from cli.health import cmd_check, cmd_status


@patch('cli.health._req')
def test_check_healthy_authenticated(mock_req):
    mock_req.side_effect = [
        (200, {"status": "healthy"}),
        (200, {"authenticated": True, "isExpired": False, "status": "正常", "nickname": "test", "fakeid": "abc"}),
    ]
    with patch('cli.health._ok') as mock_ok:
        cmd_check(auto_recover=False)
        data = mock_ok.call_args[0][0]
        assert data["service_healthy"] is True
        assert data["authenticated"] is True


@patch('cli.health._req')
def test_check_service_down(mock_req):
    mock_req.return_value = (0, {"error": "connection refused"})
    with patch('cli.health._fail') as mock_fail:
        cmd_check(auto_recover=False)
        mock_fail.assert_called_once()


@patch('cli.health._req')
def test_status(mock_req):
    mock_req.return_value = (200, {"authenticated": True, "isExpired": False, "status": "正常"})
    with patch('cli.health._ok') as mock_ok:
        cmd_status()
        data = mock_ok.call_args[0][0]
        assert data["authenticated"] is True
```

- [ ] **步骤 2：编写 `cli/health.py` 实现**

```python
"""wq check — health + login status; wq status — auth-only."""
from cli.core import _req, _ok, _fail


def cmd_check(auto_recover=False):
    code_h, health = _req("GET", "/health")
    code_s, status = _req("GET", "/admin/status")

    result = {
        "service_healthy": code_h == 200 and health.get("status") == "healthy",
        "authenticated": status.get("authenticated", False),
        "is_expired": status.get("isExpired", True),
        "login_status_text": status.get("status", "未知"),
        "nickname": status.get("nickname", ""),
        "fakeid": status.get("fakeid", ""),
        "effective_route": status.get("effective_route", ""),
    }
    if auto_recover and not result["service_healthy"]:
        import subprocess
        try:
            subprocess.run(["bash", "/home/gly/wq-cli/start.sh"], timeout=30)
            code_h2, _ = _req("GET", "/health")
            result["auto_recovered"] = code_h2 == 200
            result["service_healthy"] = code_h2 == 200
        except Exception as e:
            result["auto_recover_error"] = str(e)

    result["ok"] = result["service_healthy"] and result["authenticated"] and not result["is_expired"]
    _ok(result)


def cmd_status():
    code, status = _req("GET", "/admin/status")
    if code != 200:
        _fail(f"Failed to get status: HTTP {code}")
    _ok(status)
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_health.py -v 2>&1`
预期：全部 PASS

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/health.py tests/test_health.py
git commit -m "feat(cli): add health check and status commands"
```

---

### 任务 5：cli/search.py 公众号搜索

**文件：**
- 创建：`/home/gly/wq-cli/cli/search.py`
- 创建：`/home/gly/wq-cli/tests/test_search.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.search — search, info."""
from unittest.mock import patch
from cli.search import cmd_search, cmd_info


@patch('cli.search._req')
def test_search_returns_list(mock_req):
    mock_req.return_value = (200, {
        "success": True,
        "data": {"list": [{"fakeid": "abc", "nickname": "TestAccount", "alias": "test"}]}
    })
    with patch('cli.search._ok') as mock_ok:
        cmd_search("test", fmt="json")
        data = mock_ok.call_args[0][0]
        assert data[0]["fakeid"] == "abc"


@patch('cli.search._req')
def test_search_no_results(mock_req):
    mock_req.return_value = (200, {"success": True, "data": {"list": []}})
    with patch('cli.search._fail') as mock_fail:
        cmd_search("nobody", fmt="json")
        mock_fail.assert_called_once()


@patch('cli.search._req')
def test_info(mock_req):
    mock_req.return_value = (200, {
        "data": {"identity_name": "TestCompany", "is_verify": 2, "original_article_count": 15234}
    })
    with patch('cli.search._ok') as mock_ok:
        cmd_info("abc")
        data = mock_ok.call_args[0][0]
        assert data["identity_name"] == "TestCompany"
```

- [ ] **步骤 2：编写 `cli/search.py` 实现**

```python
"""wq search — search WeChat accounts; wq info — account details."""
import urllib.parse
from cli.core import _req, _ok, _fail


def cmd_search(query, fmt="json"):
    encoded = urllib.parse.quote(query)
    code, d = _req("GET", f"/public/searchbiz?query={encoded}")
    if code != 200:
        _fail(d.get("error", f"HTTP {code}"))
    if not d.get("success"):
        _fail(d.get("error", "Search failed"))
    accounts = d["data"]["list"]
    if not accounts:
        _fail("No matching accounts found")
    if fmt == "table":
        _ok_as_table(accounts)
    _ok(accounts)


def cmd_info(fakeid):
    code, d = _req("GET", f"/public/accountinfo?fakeid={fakeid}")
    if code != 200:
        _fail(d.get("error", f"HTTP {code}"))
    _ok(d.get("data", d))


def _ok_as_table(accounts):
    from cli.core import _format_table
    rows = [[a.get("nickname", ""), a["fakeid"], a.get("alias", ""), a.get("round_head_img", "")[:50] + "..."]
            for a in accounts]
    print(_format_table(rows, ["Nickname", "FakeID", "Alias", "HeadImg"]))
    import sys
    sys.exit(0)
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_search.py -v`
预期：全部 PASS

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/search.py tests/test_search.py
git commit -m "feat(cli): add search and info commands"
```

---

### 任务 6：cli/subscribe.py 订阅管理

**文件：**
- 创建：`/home/gly/wq-cli/cli/subscribe.py`
- 创建：`/home/gly/wq-cli/tests/test_subscribe.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.subscribe — subscribe, unsubscribe, subscriptions, poll."""
from unittest.mock import patch
from cli.subscribe import cmd_subscribe, cmd_unsubscribe, cmd_subscriptions, cmd_poll


@patch('cli.subscribe._req')
def test_subscribe(mock_req):
    mock_req.return_value = (200, {"success": True, "message": "订阅成功"})
    with patch('cli.subscribe._ok') as mock_ok:
        cmd_subscribe("abc", "TestAccount")
        data = mock_ok.call_args[0][0]
        assert data["message"] == "订阅成功"


@patch('cli.subscribe._req')
def test_unsubscribe(mock_req):
    mock_req.return_value = (200, {"success": True, "message": "已取消订阅"})
    with patch('cli.subscribe._ok') as mock_ok:
        cmd_unsubscribe("abc")
        mock_ok.assert_called_once()


@patch('cli.subscribe._req')
def test_subscriptions(mock_req):
    mock_req.return_value = (200, {"success": True, "data": [
        {"fakeid": "abc", "nickname": "Test", "article_count": 10, "rss_url": "http://..."}
    ]})
    with patch('cli.subscribe._ok') as mock_ok:
        cmd_subscriptions(fmt="json")
        data = mock_ok.call_args[0][0]
        assert data[0]["nickname"] == "Test"


@patch('cli.subscribe._req')
def test_poll(mock_req):
    mock_req.return_value = (200, {"success": True, "data": {"message": "轮询完成"}})
    with patch('cli.subscribe._ok') as mock_ok:
        cmd_poll()
        mock_ok.assert_called_once()
```

- [ ] **步骤 2：编写 `cli/subscribe.py` 实现**

```python
"""wq subscribe / unsubscribe / subscriptions / poll — RSS subscription management."""
from cli.core import _req, _ok, _fail


def cmd_subscribe(fakeid, nickname=""):
    code, d = _req("POST", "/rss/subscribe", body={"fakeid": fakeid, "nickname": nickname})
    if code != 200:
        _fail(f"Subscribe failed: HTTP {code}")
    _ok({"message": d.get("message", "Subscribed"), "fakeid": fakeid})


def cmd_unsubscribe(fakeid):
    code, d = _req("DELETE", f"/rss/subscribe/{fakeid}")
    if code != 200:
        _fail(f"Unsubscribe failed: HTTP {code}")
    _ok({"message": d.get("message", "Unsubscribed"), "fakeid": fakeid})


def cmd_subscriptions(fmt="json"):
    code, d = _req("GET", "/rss/subscriptions")
    if code != 200:
        _fail(f"List subscriptions failed: HTTP {code}")
    subs = d.get("data", [])
    if fmt == "table":
        from cli.core import _format_table
        rows = [[s.get("nickname", ""), s["fakeid"], s.get("article_count", 0), s.get("rss_url", "")]
                for s in subs]
        print(_format_table(rows, ["Nickname", "FakeID", "Articles", "RSS URL"]))
        import sys
        sys.exit(0)
    _ok(subs)


def cmd_poll():
    code, d = _req("POST", "/rss/poll")
    if code != 200:
        _fail(f"Poll failed: HTTP {code}")
    _ok(d.get("data", {"message": "Poll triggered"}))
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_subscribe.py -v`

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/subscribe.py tests/test_subscribe.py
git commit -m "feat(cli): add subscribe management commands"
```

---

### 任务 7：cli/articles.py 文章查询

**文件：**
- 创建：`/home/gly/wq-cli/cli/articles.py`
- 创建：`/home/gly/wq-cli/tests/test_articles.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.articles — query cached articles with DB/API fallback."""
import os
import sqlite3
import tempfile
from unittest.mock import patch
from cli.articles import cmd_articles


class TestArticlesDB:
    def setUp(self):
        self.tmpdir = tempfile.mkdtemp()
        self.db_path = os.path.join(self.tmpdir, "test.db")
        conn = sqlite3.connect(self.db_path)
        conn.execute("CREATE TABLE articles (id INTEGER PRIMARY KEY, fakeid TEXT, title TEXT, link TEXT, author TEXT, digest TEXT, publish_time INTEGER, fetched_at INTEGER)")
        conn.execute("CREATE TABLE subscriptions (fakeid TEXT PRIMARY KEY, nickname TEXT)")
        conn.execute("INSERT INTO subscriptions VALUES ('abc', 'TestAccount')")
        conn.execute("INSERT INTO articles VALUES (1, 'abc', 'Test Title', 'https://mp.weixin.qq.com/s/test', 'Author', 'Digest text', 1700000000, 1700000000)")
        conn.commit()
        conn.close()

    def tearDown(self):
        os.unlink(self.db_path)
        os.rmdir(self.tmpdir)


@patch('cli.articles._db_conn')
def test_articles_returns_list(mock_db):
    """When DB is available, query it directly."""
    conn = sqlite3.connect(":memory:")
    conn.row_factory = sqlite3.Row
    conn.execute("CREATE TABLE articles (id INTEGER PRIMARY KEY, fakeid TEXT, title TEXT, link TEXT, author TEXT, digest TEXT, publish_time INTEGER, fetched_at INTEGER)")
    conn.execute("CREATE TABLE subscriptions (fakeid TEXT PRIMARY KEY, nickname TEXT)")
    conn.execute("INSERT INTO subscriptions VALUES ('abc', 'TestAccount')")
    conn.execute("INSERT INTO articles VALUES (1, 'abc', 'Test', 'http://...', 'A', 'D', 1700000000, 1700000000)")
    mock_db.return_value = conn
    with patch('cli.articles._ok') as mock_ok:
        cmd_articles(fakeid=None, hours=None, keyword=None, limit=20, fmt="json")
        data = mock_ok.call_args[0][0]
        assert len(data) == 1
        assert data[0]["title"] == "Test"


@patch('cli.articles._db_conn')
@patch('cli.articles._req')
def test_articles_api_fallback(mock_req, mock_db):
    """When DB is unavailable (None), fall back to HTTP API."""
    mock_db.return_value = None
    mock_req.return_value = (200, {
        "success": True,
        "data": [{"id": 1, "fakeid": "abc", "title": "API Article", "link": "http://..."}]
    })
    with patch('cli.articles._ok') as mock_ok:
        cmd_articles(fakeid="abc", hours=None, keyword=None, limit=20, fmt="json")
        data = mock_ok.call_args[0][0]
        assert data[0]["title"] == "API Article"
```

- [ ] **步骤 2：编写 `cli/articles.py` 实现**

```python
"""wq articles — query cached articles via SQLite with API fallback."""
import time
from datetime import datetime, timezone, timedelta
from cli.core import _db_conn, _req, _ok, _fail

TZ = timezone(timedelta(hours=8), "Asia/Shanghai")


def cmd_articles(fakeid=None, hours=None, keyword=None, limit=20, fmt="json"):
    conn = _db_conn()
    if conn is not None:
        _articles_from_db(conn, fakeid, hours, keyword, limit, fmt)
    else:
        _articles_from_api(fakeid, hours, keyword, limit, fmt)


def _articles_from_db(conn, fakeid, hours, keyword, limit, fmt):
    conditions = ["1=1"]
    params = {}
    if fakeid:
        conditions.append("a.fakeid = :fakeid")
        params["fakeid"] = fakeid
    if keyword:
        conditions.append("a.title LIKE :kw")
        params["kw"] = f"%{keyword}%"
    if hours is not None:
        cutoff = int(time.time()) - int(hours) * 3600
        conditions.append("a.publish_time >= :cutoff")
        params["cutoff"] = cutoff

    sql = f"""
        SELECT a.id, a.fakeid, s.nickname, a.title, a.link, a.author,
               a.digest, a.publish_time, a.fetched_at
        FROM articles a
        LEFT JOIN subscriptions s ON a.fakeid = s.fakeid
        WHERE {' AND '.join(conditions)}
        ORDER BY a.publish_time DESC
        LIMIT :limit
    """
    params["limit"] = limit
    rows = conn.execute(sql, params).fetchall()
    conn.close()

    articles = [_row_to_dict(r) for r in rows]
    if fmt == "table":
        _articles_table(articles)
    _ok(articles)


def _articles_from_api(fakeid, hours, keyword, limit, fmt):
    """Level 4 fallback: use /api/public/articles."""
    params = []
    if fakeid:
        params.append(f"fakeid={fakeid}")
    if keyword:
        import urllib.parse
        params.append(f"keyword={urllib.parse.quote(keyword)}")
    params.append(f"count={limit}")
    qs = "&".join(params)
    code, d = _req("GET", f"/public/articles?{qs}")
    if code != 200 or not d.get("success"):
        _fail(d.get("error", f"API fallback failed: HTTP {code}"))
    articles = d.get("data", [])
    if fmt == "table":
        _articles_table(articles)
    _ok(articles)


def _row_to_dict(r):
    pub = r["publish_time"]
    return {
        "id": r["id"],
        "fakeid": r["fakeid"],
        "nickname": r["nickname"] or "",
        "title": r["title"],
        "link": r["link"],
        "author": r["author"] or "",
        "digest": (r["digest"] or "")[:200],
        "publish_time": pub,
        "publish_time_str": datetime.fromtimestamp(pub, TZ).strftime("%Y-%m-%d %H:%M") if pub else "",
    }


def _articles_table(articles):
    from cli.core import _format_table
    rows = [[a.get("nickname", ""), a["title"], a.get("publish_time_str", ""),
             (a.get("digest", "") or "")[:60]]
            for a in articles]
    print(_format_table(rows, ["Account", "Title", "Time", "Digest"]))
    import sys
    sys.exit(0)
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_articles.py -v`

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/articles.py tests/test_articles.py
git commit -m "feat(cli): add articles query with DB/API fallback"
```

---

### 任务 8：cli/fetch.py 文章抓取（JSON/MD/MHTML）

**文件：**
- 创建：`/home/gly/wq-cli/cli/fetch.py`
- 创建：`/home/gly/wq-cli/tests/test_fetch.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.fetch — fetch article with json/md/mhtml output."""
from unittest.mock import patch, MagicMock
import os
import tempfile
from cli.fetch import cmd_fetch, _WECHAT_BASE_CSS


def test_base_css_is_non_empty():
    assert "font-family" in _WECHAT_BASE_CSS
    assert "max-width" in _WECHAT_BASE_CSS


@patch('cli.fetch._req')
def test_fetch_json(mock_req):
    mock_req.return_value = (200, {
        "success": True,
        "data": {
            "title": "Test Article",
            "content": "<div>Hello</div>",
            "plain_content": "Hello",
            "author": "作者",
            "publish_time": 1700000000,
            "images": []
        }
    })
    with patch('cli.fetch._ok') as mock_ok:
        cmd_fetch("https://mp.weixin.qq.com/s/test", fmt="json", outdir=".")
        data = mock_ok.call_args[0][0]
        assert data["title"] == "Test Article"


@patch('cli.fetch._req')
@patch('cli.fetch._download_images')
def test_fetch_md(mock_dl, mock_req):
    mock_req.return_value = (200, {
        "success": True,
        "data": {
            "title": "Test MD",
            "content": "<div><p>Hello</p><img src='https://mmbiz.qpic.cn/a.jpg'></div>",
            "plain_content": "Hello",
            "author": "",
            "publish_time": 1700000000,
            "images": ["https://mmbiz.qpic.cn/a.jpg"]
        }
    })
    mock_dl.return_value = {"https://mmbiz.qpic.cn/a.jpg": "/tmp/img/abc.jpg"}
    tmpdir = tempfile.mkdtemp()
    try:
        with patch('cli.fetch._ok') as mock_ok:
            cmd_fetch("https://mp.weixin.qq.com/s/test", fmt="md", outdir=tmpdir)
            data = mock_ok.call_args[0][0]
            assert "md_path" in data
            assert os.path.exists(data["md_path"])
    finally:
        pass


@patch('cli.fetch._req')
@patch('cli.fetch._download_images')
def test_fetch_mhtml(mock_dl, mock_req):
    mock_req.return_value = (200, {
        "success": True,
        "data": {
            "title": "Test MHTML",
            "content": "<div><p>Hello</p><img src='https://mmbiz.qpic.cn/b.jpg'></div>",
            "plain_content": "Hello",
            "author": "",
            "publish_time": 1700000000,
            "images": ["https://mmbiz.qpic.cn/b.jpg"]
        }
    })
    mock_dl.return_value = {"https://mmbiz.qpic.cn/b.jpg": "/tmp/img/def.jpg"}
    tmpdir = tempfile.mkdtemp()
    try:
        with patch('cli.fetch._ok') as mock_ok:
            cmd_fetch("https://mp.weixin.qq.com/s/test", fmt="mhtml", outdir=tmpdir)
            data = mock_ok.call_args[0][0]
            assert "mhtml_path" in data
            assert os.path.exists(data["mhtml_path"])
    finally:
        pass
```

- [ ] **步骤 2：编写 `cli/fetch.py` 实现**

```python
"""wq fetch — fetch article full content with json/md/mhtml output."""
import base64
import hashlib
import os
import re
import sys
import uuid
from datetime import datetime, timezone, timedelta
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText
from email.mime.image import MIMEImage

from cli.core import _req, _ok, _fail, _download_images

TZ = timezone(timedelta(hours=8), "Asia/Shanghai")

_WECHAT_BASE_CSS = """\
body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
       "Hiragino Sans GB", "Microsoft YaHei", sans-serif; font-size: 17px;
       line-height: 1.6; color: #333; max-width: 677px; margin: 0 auto;
       padding: 20px; }
img { max-width: 100%; height: auto; }
p { margin: 0 0 1em; }
h1, h2, h3 { margin: 1.2em 0 0.6em; }
blockquote { border-left: 3px solid #ddd; padding-left: 1em; color: #666; margin: 1em 0; }
pre { background: #f5f5f5; padding: 1em; overflow-x: auto; border-radius: 4px; }
code { background: #f5f5f5; padding: 0.2em 0.4em; border-radius: 3px; font-size: 0.9em; }
"""


def cmd_fetch(url, fmt="json", outdir="."):
    code, d = _req("POST", "/article", body={"url": url})
    if code != 200:
        _fail(f"Fetch failed: HTTP {code}")
    if not d.get("success"):
        _fail(d.get("error", "Fetch failed"))

    article = d["data"]
    if fmt == "json":
        _ok(article)
    elif fmt == "md":
        _ok(_fetch_md(article, outdir))
    elif fmt == "mhtml":
        _ok(_fetch_mhtml(article, outdir))


def _fetch_md(article, outdir):
    """Render article as Markdown with local images."""
    title = article.get("title", "untitled")
    content = article.get("content", "")
    images = article.get("images", [])

    # Check writability
    outpath = os.path.abspath(outdir)
    if not os.access(outpath, os.W_OK):
        _fail(f"Output directory not writable: {outpath}")

    # Download images
    img_dir = os.path.join(outpath, "images")
    img_map = {}
    if images:
        img_map = _download_images(images, img_dir)

    # Convert HTML to Markdown
    md_body = _html_to_md(content)

    # Replace image URLs with local paths
    for url, local_path in img_map.items():
        rel_path = os.path.relpath(local_path, outpath)
        md_body = md_body.replace(url, rel_path)

    # Build article markdown
    author = article.get("author", "")
    pub_ts = article.get("publish_time", 0)
    pub_str = datetime.fromtimestamp(pub_ts, TZ).strftime("%Y-%m-%d %H:%M:%S") if pub_ts else ""

    lines = [f"# {title}", ""]
    if author:
        lines.append(f"**作者**: {author}")
    if pub_str:
        lines.append(f"**发布时间**: {pub_str}")
    lines.append("")
    lines.append(md_body)

    # Safe filename
    safe_title = re.sub(r'[<>:"/\\|?*]', '_', title)[:80]
    md_path = os.path.join(outpath, f"{safe_title}.md")
    with open(md_path, 'w', encoding='utf-8') as f:
        f.write("\n".join(lines))

    return {"md_path": md_path, "image_dir": img_dir}


def _fetch_mhtml(article, outdir):
    """Render article as MHTML with inline images."""
    title = article.get("title", "untitled")
    content = article.get("content", "")
    images = article.get("images", [])

    outpath = os.path.abspath(outdir)
    if not os.access(outpath, os.W_OK):
        _fail(f"Output directory not writable: {outpath}")

    # Download images
    img_dir = os.path.join(outpath, "images")
    img_map = {}
    if images:
        img_map = _download_images(images, img_dir)

    # Build MIME multipart
    boundary = f"----wq-mhtml-{uuid.uuid4().hex[:16]}"
    mhtml = MIMEMultipart("related", boundary=boundary)

    # Extract <style> blocks from original HTML
    style_blocks = []
    for m in re.finditer(r'<style[^>]*>(.*?)</style>', content, re.DOTALL | re.IGNORECASE):
        style_blocks.append(m.group(1))

    combined_css = "\n".join(style_blocks) + "\n" + _WECHAT_BASE_CSS

    # Build HTML body with cid: references
    html_body = content
    cid_map = {}
    for i, (url, local_path) in enumerate(img_map.items()):
        cid = f"image-{i:03d}"
        cid_map[cid] = (url, local_path)
        html_body = html_body.replace(url, f"cid:{cid}")

    html_doc = f"""<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{_escape_html(title)}</title>
  <base href="https://mp.weixin.qq.com/">
  <style>{combined_css}</style>
</head>
<body>
{html_body}
</body>
</html>"""

    html_part = MIMEText(html_doc, "html", "utf-8")
    mhtml.attach(html_part)

    # Attach images as base64
    for cid, (url, local_path) in cid_map.items():
        with open(local_path, 'rb') as f:
            img_data = f.read()
        ext = os.path.splitext(local_path)[1].lower()
        mime_type = {
            '.jpg': 'jpeg', '.jpeg': 'jpeg', '.png': 'png',
            '.gif': 'gif', '.webp': 'webp', '.svg': 'svg+xml'
        }.get(ext, 'jpeg')
        img_part = MIMEImage(img_data, _subtype=mime_type.split('+')[-1])
        img_part.add_header("Content-ID", f"<{cid}>")
        img_part.add_header("Content-Transfer-Encoding", "base64")
        mhtml.attach(img_part)

    safe_title = re.sub(r'[<>:"/\\|?*]', '_', title)[:80]
    mhtml_path = os.path.join(outpath, f"{safe_title}.mhtml")
    with open(mhtml_path, 'w', encoding='utf-8') as f:
        f.write(mhtml.as_string())

    return {"mhtml_path": mhtml_path}


def _html_to_md(html):
    """Convert HTML to Markdown. Uses html2text if available, else basic strip."""
    try:
        import html2text
        h = html2text.HTML2Text()
        h.body_width = 0
        h.ignore_links = False
        h.ignore_images = False
        return h.handle(html)
    except ImportError:
        # Fallback: basic tag stripping
        text = re.sub(r'<br\s*/?\s*>', '\n', html, flags=re.IGNORECASE)
        text = re.sub(r'</(?:p|div|section|h[1-6]|tr|li|blockquote)>', '\n', text, flags=re.IGNORECASE)
        text = re.sub(r'<[^>]+>', '', text)
        text = re.sub(r'\n{3,}', '\n\n', text)
        return text.strip()


def _escape_html(s):
    return s.replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;').replace('"', '&quot;')
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_fetch.py -v`

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/fetch.py tests/test_fetch.py
git commit -m "feat(cli): add fetch command with json/md/mhtml output"
```

---

### 任务 9：cli/push.py 推送报告

**文件：**
- 创建：`/home/gly/wq-cli/cli/push.py`
- 创建：`/home/gly/wq-cli/tests/test_push.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.push — push-report, md-push."""
import sqlite3
import time
from unittest.mock import patch
from cli.push import cmd_push_report, cmd_md_push


@patch('cli.push._db_conn')
@patch('cli.push._req')
def test_push_report(mock_req, mock_db):
    conn = sqlite3.connect(":memory:")
    conn.row_factory = sqlite3.Row
    conn.execute("CREATE TABLE articles (id INTEGER PRIMARY KEY, fakeid TEXT, title TEXT, link TEXT, author TEXT, digest TEXT, publish_time INTEGER, fetched_at INTEGER)")
    conn.execute("CREATE TABLE subscriptions (fakeid TEXT PRIMARY KEY, nickname TEXT)")
    conn.execute("INSERT INTO subscriptions VALUES ('abc', 'Test')")
    conn.execute("INSERT INTO articles VALUES (1, 'abc', 'T', 'http://...', 'A', 'D', ?,
    ?)", (int(time.time()) - 3600, int(time.time()) - 3600))
    mock_db.return_value = conn
    mock_req.return_value = (200, {"authenticated": True, "isExpired": False, "status": "正常"})
    with patch('cli.push._ok') as mock_ok:
        cmd_push_report(hours=24)
        data = mock_ok.call_args[0][0]
        assert data["article_count"] == 1
        assert data["period_hours"] == 24


@patch('cli.push._db_conn')
@patch('cli.push._req')
def test_md_push(mock_req, mock_db):
    conn = sqlite3.connect(":memory:")
    conn.row_factory = sqlite3.Row
    conn.execute("CREATE TABLE articles (id, fakeid, title, link, author, digest, publish_time, fetched_at)")
    conn.execute("CREATE TABLE subscriptions (fakeid TEXT PRIMARY KEY, nickname TEXT)")
    conn.execute("INSERT INTO subscriptions VALUES ('abc', 'Test')")
    mock_db.return_value = conn
    mock_req.return_value = (200, {"authenticated": True, "isExpired": False, "status": "正常"})
    with patch('builtins.print') as mock_print:
        cmd_md_push(hours=24)
        mock_print.assert_called()
```

- [ ] **步骤 2：编写 `cli/push.py` 实现**

```python
"""wq push-report / md-push — generate article push reports."""
import time
from datetime import datetime, timezone, timedelta
from cli.core import _db_conn, _req, _ok, _fail

TZ = timezone(timedelta(hours=8), "Asia/Shanghai")


def cmd_push_report(hours=24):
    conn = _db_conn()
    if not conn:
        _fail("Database not found. Check service is running at " + __import__('os').environ.get("WECHAT_SERVICE_DIR", "/home/gly/wq-cli"))

    cutoff = int(time.time()) - hours * 3600
    rows = conn.execute(
        """SELECT a.id, a.fakeid, s.nickname, a.title, a.link, a.digest,
                  a.publish_time
           FROM articles a
           LEFT JOIN subscriptions s ON a.fakeid = s.fakeid
           WHERE a.publish_time >= :cutoff
           ORDER BY s.nickname, a.publish_time DESC
           LIMIT 200""",
        {"cutoff": cutoff},
    ).fetchall()
    conn.close()

    articles = []
    for r in rows:
        pub = r["publish_time"]
        articles.append({
            "id": r["id"],
            "nickname": r["nickname"] or "",
            "title": r["title"],
            "link": r["link"],
            "digest": (r["digest"] or "")[:300],
            "publish_time_str": datetime.fromtimestamp(pub, TZ).strftime("%Y-%m-%d %H:%M") if pub else "",
        })

    _, status = _req("GET", "/admin/status")
    login_info = {
        "authenticated": status.get("authenticated", False),
        "is_expired": status.get("isExpired", True),
        "status_text": status.get("status", ""),
    }

    _ok({
        "period_hours": hours,
        "end_time": datetime.now(TZ).strftime("%Y-%m-%d %H:%M:%S"),
        "start_time": datetime.fromtimestamp(cutoff, TZ).strftime("%Y-%m-%d %H:%M:%S"),
        "article_count": len(articles),
        "articles": articles,
        "login": login_info,
    })


def cmd_md_push(hours=24):
    conn = _db_conn()
    if not conn:
        print("Database not found")
        import sys
        sys.exit(1)

    cutoff = int(time.time()) - hours * 3600
    rows = conn.execute(
        """SELECT a.id, a.fakeid, s.nickname, a.title, a.link, a.digest,
                  a.publish_time
           FROM articles a
           LEFT JOIN subscriptions s ON a.fakeid = s.fakeid
           WHERE a.publish_time >= :cutoff
           ORDER BY a.publish_time DESC""",
        {"cutoff": cutoff},
    ).fetchall()
    conn.close()

    _, status = _req("GET", "/admin/status")

    now_str = datetime.now(TZ).strftime("%Y-%m-%d %H:%M:%S")
    lines = [f"wq 公众号文章推送 — {now_str}", ""]

    if not rows:
        lines.append(f"最近 {hours} 小时无新增文章。")
    else:
        by_account = {}
        for r in rows:
            name = r["nickname"] or r["fakeid"][:8]
            by_account.setdefault(name, []).append(r)

        for name, arts in sorted(by_account.items()):
            lines.append(f"**【{name}】** — {len(arts)} 篇")
            for a in arts:
                pub = a["publish_time"]
                ts = datetime.fromtimestamp(pub, TZ).strftime("%m-%d %H:%M") if pub else ""
                digest = (a["digest"] or "")[:120]
                lines.append(f"• {a['title']}  _{ts}_")
                lines.append(f"  {a['link']}")
                if digest:
                    lines.append(f"  > {digest}")
            lines.append("")

    lines.append("---")
    authed = status.get("authenticated", False)
    s_text = status.get("status", "未知")
    is_expired = status.get("isExpired", True)
    lines.append(f"**登录状态**: {'OK' if authed else 'EXPIRED'}")
    lines.append(f"Status: {s_text}")
    if not authed or is_expired:
        lines.append("")
        lines.append("Login expired, please visit http://localhost:5000/login.html to re-scan QR code.")

    print("\n".join(lines))
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_push.py -v`

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/push.py tests/test_push.py
git commit -m "feat(cli): add push-report and md-push commands"
```

---

### 任务 10：cli/login.py 登录引导

**文件：**
- 创建：`/home/gly/wq-cli/cli/login.py`
- 创建：`/home/gly/wq-cli/tests/test_login.py`

- [ ] **步骤 1：编写测试**

```python
"""Tests for cli.login."""
from cli.login import cmd_login


def test_login_output():
    import io
    import sys
    captured = io.StringIO()
    old_stdout = sys.stdout
    sys.stdout = captured
    try:
        cmd_login()
    except SystemExit:
        pass
    finally:
        sys.stdout = old_stdout
    output = captured.getvalue()
    assert "http://localhost:5000/login.html" in output
    assert "wq check" in output
```

- [ ] **步骤 2：编写 `cli/login.py` 实现**

```python
"""wq login — guide user to browser-based QR code login."""
import sys
from cli.core import _req


def cmd_login():
    """Print login page URL and optional expiry warning."""
    code, status = _req("GET", "/admin/status")
    print("Login page: http://localhost:5000/login.html")
    if code == 200:
        is_expired = status.get("isExpired", False)
        authenticated = status.get("authenticated", False)
        if not authenticated:
            print("\nStatus: NOT LOGGED IN — please scan QR code on the page above.")
        elif is_expired:
            print("\nStatus: LOGIN EXPIRED — please re-scan QR code on the page above.")
        else:
            nickname = status.get("nickname", "(unknown)")
            expire_ts = status.get("expireTime", 0)
            if expire_ts > 1e12:
                expire_ts = expire_ts / 1000
            from datetime import datetime, timezone, timedelta
            TZ = timezone(timedelta(hours=8), "Asia/Shanghai")
            expire_str = datetime.fromtimestamp(expire_ts, TZ).strftime("%Y-%m-%d %H:%M") if expire_ts else "unknown"
            print(f"\nStatus: LOGGED IN as {nickname}, expires at {expire_str}")
    else:
        print(f"\nWARNING: Service unreachable (HTTP {code}). Start it first: bash /home/gly/wq-cli/start.sh")
    sys.exit(0)
```

- [ ] **步骤 3：运行测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/test_login.py -v`

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add cli/login.py tests/test_login.py
git commit -m "feat(cli): add login command with browser QR code guide"
```

---

### 任务 11：Agent Skill — OpenClaw 版本

> **重要**: 使用 `Skill("writing-skills")` 创建此 Skill。

**文件：**
- 创建：`/home/gly/wq-cli/skills/openclaw/SKILL.md`
- 创建：`/home/gly/wq-cli/skills/openclaw/opencli.json`
- 创建：`/home/gly/wq-cli/skills/openclaw/references/troubleshooting.md`

- [ ] **步骤 1：创建共享 reference 文件**

先创建 `skills/shared/commands-schema.json`：

```json
{
  "version": "0.1.0",
  "commands": {
    "check": {
      "description": "Health + login status check",
      "args": [],
      "options": ["--auto-recover"],
      "output": {"ok": "bool", "data": {"service_healthy": "bool", "authenticated": "bool", "is_expired": "bool"}}
    },
    "search": {
      "description": "Search WeChat Official Accounts",
      "args": ["<query: str>"],
      "options": ["--format=json|table"],
      "output": {"ok": "bool", "data": "[{fakeid, nickname, alias, round_head_img}]"}
    },
    "info": {
      "description": "Get account detail info",
      "args": ["<fakeid: str>"],
      "output": {"ok": "bool", "data": {"identity_name": "str", "is_verify": "int", "original_article_count": "int"}}
    },
    "subscribe": {
      "description": "Add RSS subscription",
      "args": ["<fakeid: str>"],
      "options": ["--nickname=str"],
      "output": {"ok": "bool", "data": {"message": "str", "fakeid": "str"}}
    },
    "unsubscribe": {
      "description": "Remove RSS subscription",
      "args": ["<fakeid: str>"],
      "output": {"ok": "bool", "data": {"message": "str"}}
    },
    "subscriptions": {
      "description": "List all subscriptions",
      "options": ["--format=json|table"],
      "output": {"ok": "bool", "data": "[{fakeid, nickname, article_count, rss_url}]"}
    },
    "poll": {
      "description": "Trigger RSS poll",
      "output": {"ok": "bool", "data": {"message": "str"}}
    },
    "articles": {
      "description": "Query cached articles",
      "options": ["--hours=N", "--keyword=K", "--fakeid=F", "--limit=N", "--format=json|table"],
      "output": {"ok": "bool", "data": "[{id, nickname, title, link, digest, publish_time_str}]"}
    },
    "fetch": {
      "description": "Fetch article full content with format options",
      "args": ["<url: str>"],
      "options": ["--format=json|md|mhtml", "--outdir=PATH"],
      "output_json": {"ok": "bool", "data": {"title": "str", "content": "str", "plain_content": "str"}},
      "output_md": "article.md + images/ directory",
      "output_mhtml": "article.mhtml (offline archive with inline images)"
    },
    "push-report": {
      "description": "Generate JSON push report",
      "options": ["--hours=N"],
      "output": {"ok": "bool", "data": {"article_count": "int", "articles": "[...]"}}
    },
    "md-push": {
      "description": "Generate Markdown push report (non-JSON output)",
      "options": ["--hours=N"],
      "output": "Pure Markdown text (not JSON)"
    },
    "login": {
      "description": "Show login page URL",
      "output": "Plain text with URL"
    },
    "cron-setup": {
      "description": "Print cron registration guide",
      "output": "Plain text instructions"
    },
    "version": {
      "description": "Show version",
      "output": "Plain text"
    }
  }
}
```

再创建 `skills/shared/article-formats.md`：

```markdown
# Article Output Formats

## JSON (default)

`wq fetch <url>` — default JSON output, same as `POST /api/article` response.

## Markdown (`--format=md`)

`wq fetch <url> --format=md --outdir=./output`

- Converts HTML to Markdown (html2text)
- Downloads all images to `--outdir/images/`
- Replaces image URLs with local relative paths
- Output: `output/<title>.md` + `output/images/`

## MHTML (`--format=mhtml`)

`wq fetch <url> --format=mhtml --outdir=./output`

- Full offline archive in single .mhtml file
- Images embedded as base64 (cid: references)
- WeChat CSS baseline inlined
- Output: `output/<title>.mhtml`
```

- [ ] **步骤 2：使用 writing-skills 创建 OpenClaw SKILL.md**

调用 `Skill("writing-skills")`，内容如设计规格 5.2 节所示。包括：
- frontmatter (tags, triggers, tool_groups: [reader, monitor])
- L1: 触发词匹配
- L2: 命令速查表（wq 代码块）
- L3 引用: references/commands.md, references/formats.md, references/troubleshooting.md

- [ ] **步骤 3：创建 opencli.json**

使用任务中定义的 opencli.json manifest（见设计规格 5.2 节）。

- [ ] **步骤 4：创建 references/troubleshooting.md**

```markdown
# Troubleshooting

## Exit Codes

| Code | Meaning | Action |
|------|---------|--------|
| 0 | Success | — |
| 1 | General error | Check error message in JSON |
| 2 | Auth expired | Open http://localhost:5000/login.html and re-scan QR |

## Common Errors

### "Service unreachable"
```bash
bash /home/gly/wq-cli/start.sh
wq check
```

### "Not logged in" / "Auth expired"
```bash
wq login
# Open the URL in browser and scan QR code with WeChat
```

### "Rate limited"
Wait 30-60 seconds before retrying.

### "Verification required"
Open the article URL in browser and complete WeChat CAPTCHA.

## DB Permission Issues

If `wq articles` returns "database not found":
```bash
# Option 1: direct fix
sudo usermod -aG wechat-api $(whoami)
# Option 2: use API fallback (automatic in wq articles)
```
```

- [ ] **步骤 5：创建符号链接**

```bash
cd /home/gly/wq-cli/skills/openclaw/references
ln -sf ../../shared/commands-schema.json commands.md
ln -sf ../../shared/article-formats.md formats.md
```

- [ ] **步骤 6：Commit**

```bash
cd /home/gly/wq-cli
git add skills/
git commit -m "feat(skills): add OpenClaw agent skill with opencli.json manifest"
```

---

### 任务 12：Agent Skill — Hermes 版本

> **重要**: 使用 `Skill("writing-skills")` 创建此 Skill。

**文件：**
- 创建：`/home/gly/wq-cli/skills/hermes/SKILL.md`
- 创建：`/home/gly/wq-cli/skills/hermes/references/cron-setup.md`

- [ ] **步骤 1：使用 writing-skills 创建 Hermes SKILL.md**

调用 `Skill("writing-skills")`，内容如设计规格 5.3 节所示。包括：
- Hermes metadata (hermes.tags, hermes 字段格式)
- 旧脚本 deprecated 标注（日期 + 保留 7 天）
- `md-push` 显式 "⚠️ 非 JSON 输出" 标注
- venv 绝对路径 `/home/gly/wq-cli/venv/bin/wq`

- [ ] **步骤 2：创建 references/cron-setup.md**

```markdown
# Cron 任务配置

## Hermes Cron 语法

### 每日 09:00 服务巡检
```
cronjob action=create \
  name="wechat-inspection" \
  schedule="0 9 * * *" \
  script=/home/gly/wq-cli/venv/bin/wq check \
  no_agent=true \
  deliver=origin
```

### 每日 18:00 文章推送
```
cronjob action=create \
  name="wechat-daily-push" \
  schedule="0 18 * * *" \
  script=/home/gly/wq-cli/venv/bin/wq md-push --hours=24 \
  no_agent=true \
  deliver=origin
```

## OpenCLI Cron 语法 (参考)

```
opencli cron create --name wechat-check --schedule "0 9 * * *" -- wq check
opencli cron create --name wechat-push --schedule "0 18 * * *" -- wq md-push
```

## 验证
```
cronjob action=list          # Hermes
opencli cron list             # OpenCLI
```

> wq venv path: `/home/gly/wq-cli/venv/bin/wq`
```

- [ ] **步骤 3：创建符号链接**

```bash
cd /home/gly/wq-cli/skills/hermes/references
ln -sf ../../shared/commands-schema.json commands.md
ln -sf ../../shared/article-formats.md formats.md
```

- [ ] **步骤 4：Commit**

```bash
cd /home/gly/wq-cli
git add skills/hermes/
git commit -m "feat(skills): add Hermes agent skill with venv path refs"
```

---

### 任务 13：Hermes 旧文件修改

**文件：**
- 修改：`~/.hermes/skills/wechat-query/SKILL.md`
- 修改：`~/.hermes/scripts/wechat-inspection.sh`
- 修改：`~/.hermes/scripts/wechat-daily-push.sh`

- [ ] **步骤 1：更新 SKILL.md — 替换 wq 调用路径**

读取 `~/.hermes/skills/wechat-query/SKILL.md`，将：
```
python3 $WQ check
```
替换为：
```
wq check
```
同时将 `export WQ=~/.hermes/.../wechat-query.py` 改为注释形式的 deprecated 标注：
```
# ⚠️ 旧入口已废弃 (2026-05-22, 保留至 2026-05-29)
# export WQ=~/.hermes/skills/wechat-query/scripts/wechat-query.py
# 新入口: wq (pip install -e /home/gly/wq-cli)
```

- [ ] **步骤 2：更新 wechat-inspection.sh**

```bash
#!/bin/bash
# wechat-inspection.sh — daily service health check
# Updated 2026-05-22: use wq CLI
/home/gly/wq-cli/venv/bin/wq check
```

- [ ] **步骤 3：更新 wechat-daily-push.sh**

```bash
#!/bin/bash
# wechat-daily-push.sh — daily article push
# Updated 2026-05-22: use wq CLI
/home/gly/wq-cli/venv/bin/wq md-push --hours=24
```

- [ ] **步骤 4：Commit** (在 wq-cli repo 中只记录变更说明)

```bash
cd /home/gly/wq-cli
git add -A
git commit -m "chore: update Hermes skill refs and cron scripts to use wq CLI"
```

---

### 任务 14：回归验证

- [ ] **步骤 1：全量单元测试**

运行：`/home/gly/wq-cli/venv/bin/pytest tests/ -v 2>&1 | tail -30`
预期：全部 PASS (约 25+ 个测试)

- [ ] **步骤 2：CLI 命令端到端验证**（需服务运行）

```bash
# 假设 wq-cli 服务在 localhost:5000 运行
/home/gly/wq-cli/venv/bin/wq check 2>&1 | head -5
/home/gly/wq-cli/venv/bin/wq version 2>&1
/home/gly/wq-cli/venv/bin/wq search test --format=json 2>&1 | head -10
/home/gly/wq-cli/venv/bin/wq subscriptions --format=table 2>&1 | head -10
/home/gly/wq-cli/venv/bin/wq articles --hours=48 --limit=5 2>&1 | head -10
/home/gly/wq-cli/venv/bin/wq md-push --hours=48 2>&1 | head -20
/home/gly/wq-cli/venv/bin/wq cron-setup 2>&1 | head -10
```

- [ ] **步骤 3：与旧 wechat-query.py 输出对比**

运行两个命令对比 JSON schema：
```bash
# 旧脚本
python3 ~/.hermes/skills/wechat-query/scripts/wechat-query.py check 2>&1
# 新 wq
/home/gly/wq-cli/venv/bin/wq check 2>&1
# 验证：JSON keys 一致 (service_healthy, authenticated, is_expired, ...)
```

- [ ] **步骤 4：MHTML 输出验证**

```bash
mkdir -p /tmp/wq-test
/home/gly/wq-cli/venv/bin/wq fetch "https://mp.weixin.qq.com/s/<real-article-id>" --format=mhtml --outdir=/tmp/wq-test
# 验证输出文件存在且是有效 MHTML
file /tmp/wq-test/*.mhtml
grep -c "Content-Type:" /tmp/wq-test/*.mhtml
```

- [ ] **步骤 5：Commit**

```bash
cd /home/gly/wq-cli
git add -A
git commit -m "test: add regression verification for all CLI commands"
```

---

### 任务 15：收尾

- [ ] **步骤 1：标记旧 wechat-query.py 为 deprecated**

```bash
cd /home/gly/wq-cli
# 在旧脚本顶部添加 deprecated 注释
sed -i '1s/^/# DEPRECATED (2026-05-22): use wq command instead. Removed after 2026-05-29.\n/' ~/.hermes/skills/wechat-query/scripts/wechat-query.py
```

- [ ] **步骤 2：确认 pyproject.toml 版本号**

验证 `pyproject.toml` 中 `version = "0.1.0"`

- [ ] **步骤 3：最终验证**

```bash
/home/gly/wq-cli/venv/bin/pip install -e /home/gly/wq-cli
/home/gly/wq-cli/venv/bin/wq version
/home/gly/wq-cli/venv/bin/wq --help
```

- [ ] **步骤 4：Commit 所有剩余变更**

```bash
cd /home/gly/wq-cli
git status
git add -A
git commit -m "chore: bump version 0.1.0, deprecation notice, final cleanup"
```

---

## 任务依赖关系

```
任务1 (pyproject.toml)
  ↓
任务2 (core.py + test)
  ↓
任务3 (__init__.py argparse)
  ↓
任务4-10 (6个命令模块 + tests) — 可并行开发
  ├─ 4: health
  ├─ 5: search
  ├─ 6: subscribe
  ├─ 7: articles
  ├─ 8: fetch
  ├─ 9: push
  └─ 10: login
  ↓
任务11 (OpenClaw Skill) + 任务12 (Hermes Skill) — 可并行
  ↓
任务13 (旧文件修改)
  ↓
任务14 (回归验证)
  ↓
任务15 (收尾)
```

---

## 自检

**1. 规格覆盖度**: 检查设计规格的每个章节：
- 架构概览 → 任务1-3 (pyproject.toml + core + argparse)
- CLI 包设计 (module boundary) → 任务2-10
- 命令全集 (15个命令) → 任务4-10 覆盖全部
- MD/MHTML 输出 → 任务8 (fetch.py)
- Agent Skill 双版本 → 任务11-12
- OpenCLI 兼容 (parse_known_args) → 任务3
- DB 四级回落 → 任务2 (core.py: _db_conn)
- 安全设计 (chmod 0o600, 白名单, magic bytes) → 任务2 (core.py: _download_images)
- 实施计划 phases → 任务1-15 对齐

**2. 占位符扫描**: 已检查。无 TODO/待定/placeholder。所有代码步骤包含实际代码。

**3. 类型一致性**: 
- `_req(method, path, body)` → 所有命令模块一致调用
- `_ok(data)` / `_fail(msg, code)` → 一致
- `_db_conn()` 返回 `sqlite3.Connection | None` → articles.py 和 push.py 正确处理 None
- `_download_images(urls, outdir)` → fetch.py 一致调用
- `fmt` 参数 → 全部使用 `fmt` 而非 `format` (避免 shadow builtin)
